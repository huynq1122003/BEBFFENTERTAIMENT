<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Danh sách thông báo</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
      background: #f4f6f9;
    }
    main {
      margin-left: 240px;
      padding: 20px;
    }
    table img {
      max-width: 120px;
      border-radius: 6px;
    }
    .actions {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 15px;
    }
    .actions input {
      width: 250px;
    }
  </style>
</head>
<body>
  <%- include("inc/header", { user: user }) %>

  <main>
    <h2>Danh sách thông báo</h2>

    <div class="actions">
      <input type="text" id="searchBox" class="form-control" placeholder="Tìm thông báo...">
      <button class="btn btn-success" data-bs-toggle="modal" data-bs-target="#addNotiModal">+ Thêm thông báo</button>
    </div>

    <table id="notiTable" class="table table-bordered bg-white shadow-sm">
      <thead class="table-light">
        <tr>
          <th>Ngày</th>
          <th>Tiêu đề</th>
          <th>Nội dung</th>
          <th>Hình ảnh</th>
        </tr>
      </thead>
      <tbody>
        <% notifications.forEach(noti => { %>
          <tr>
           <td>
  <% if (noti.Date && noti.Date.toDate) { %>
    <%= noti.Date.toDate().toLocaleDateString("vi-VN") %>
  <% } else { %>
    <%= noti.Date || "" %>
  <% } %>
</td>
            <td><%= noti.title %></td>
            <td><%= noti.content %></td>
            <td>
              <% if (noti.image) { %>
                <img src="<%= noti.image %>" alt="image">
              <% } else { %>
                Không có ảnh
              <% } %>
            </td>
             <td>
          <a href="javascript:void(0)" 
   class="btn btn-sm btn-danger"
   data-id="<%= noti.id %>"
   onclick="confirmDeleteNoti(this)">
   🗑️
</a>

        </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </main>

  <!-- Modal Add Notification -->
  <div class="modal fade" id="addNotiModal" tabindex="-1">
    <div class="modal-dialog modal-lg">
      <div class="modal-content">
        <form action="/notifications/add" method="POST" enctype="multipart/form-data">
          <div class="modal-header">
            <h5 class="modal-title">Thêm Thông Báo Mới</h5>
            <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
          </div>
          <div class="modal-body">
            <div class="mb-3">
              <label class="form-label">Tiêu đề</label>
              <input type="text" name="title" class="form-control" required>
            </div>
            <div class="mb-3">
              <label class="form-label">Nội dung</label>
              <textarea name="content" class="form-control" rows="4" required></textarea>
            </div>
            <div class="mb-3">
              <label class="form-label">Ngày</label>
              <input type="date" name="Date" class="form-control" required>
            </div>
            <div class="mb-3">
              <label class="form-label">Ảnh</label>
              <input type="file" name="image" accept="image/*" class="form-control">
            </div>
          </div>
          <div class="modal-footer">
            <button type="submit" class="btn btn-success">Lưu</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
  function confirmDeleteNoti(btn) {
    const id = btn.getAttribute("data-id");
    Swal.fire({
      title: "Xác nhận xoá?",
      text: "Bạn có chắc chắn muốn xoá thông báo này?",
      icon: "warning",
      showCancelButton: true,
      confirmButtonText: "Có, xoá",
      cancelButtonText: "Hủy",
      confirmButtonColor: "#e74c3c",
      cancelButtonColor: "#6c757d"
    }).then((result) => {
      if (result.isConfirmed) {
        window.location.href = "/notifications/delete/" + id;
      }
    });
  }
</script>

  <script>
    // Search filter
    const searchBox = document.getElementById("searchBox");
    const rows = document.querySelectorAll("#notiTable tbody tr");
    searchBox.addEventListener("keyup", () => {
      const text = searchBox.value.toLowerCase();
      rows.forEach(row => {
        const rowText = row.innerText.toLowerCase();
        row.style.display = rowText.includes(text) ? "" : "none";
      });
    });
  </script>
</body>
</html>
